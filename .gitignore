node_modules/
dist/
coverage/
playwright-report/
test-results/
.env
.env.*
!.env.example
npm-debug.log*
*.log
.DS_Store
Thumbs.db
.tools/
functions/lib/
.codex-remote-attachments/

# Capacitor generates per-workstation native projects. Do not commit them —
# they encode local paths and drift constantly. Recreate with
# `npm run cap:add:ios` / `npm run cap:add:android`.
ios/
android/
.capacitor/
